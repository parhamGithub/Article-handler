import _construct from "@babel/runtime/helpers/esm/construct";
import _toConsumableArray from "@babel/runtime/helpers/esm/toConsumableArray";
import _toArray from "@babel/runtime/helpers/esm/toArray";
import { toGregorian } from "../../_jalali/index.js";
/**
 *
 * @param args
 * @returns {Date}
 */
export default function newDate() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  if (args.length > 1) {
    var _ref = args,
      _ref2 = _toArray(_ref),
      year = _ref2[0],
      month = _ref2[1],
      _ref2$ = _ref2[2],
      day = _ref2$ === void 0 ? 1 : _ref2$,
      rest = _ref2.slice(3);
    var g = toGregorian(year, month + 1, day);
    return _construct(Date, _toConsumableArray([g.gy, g.gm - 1, g.gd].concat(_toConsumableArray(rest))));
  }
  return _construct(Date, _toConsumableArray(args));
}